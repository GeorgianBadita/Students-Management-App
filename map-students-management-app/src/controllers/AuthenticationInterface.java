package controllers;

import javafx.stage.Stage;

public interface AuthenticationInterface {
    /**
     * Function to get the interface's stage
     * @return - interface stage
     */
    Stage getStage();
}
